# {Project Name} — Changelog

Append-only history for `idea.md`. Newest entry at the top. Nothing here is
binding — `idea.md` states the current design, this file states how it got
there. When the two disagree, `idea.md` wins and this file records that it was
superseded.

Write an entry whenever a decision is made, reversed, or narrowed; whenever a
reviewer finding changes the design; and whenever text is removed from `idea.md`
for being history rather than design. Do not annotate `idea.md` itself — move
the annotation here.

---

## {YYYY-MM-DD} — {short title of the ruling}

**Ruling (user's words):** {quote the user's own wording verbatim — this is the
one thing no later reviewer can reconstruct, since no reviewer sees the
conversation}

**Evidence:** {what the ruling rested on — a measurement, a file, a prior run, a
reviewer verdict path. Cite the file. "It seemed better" is not evidence.}

**Alternatives considered:** {the options that lost, and the one-line reason each
lost. This is the column `idea.md`'s Decisions table deliberately does not have.}

**Replaced text:** {the `idea.md` text this ruling superseded, verbatim, when
losing it would make a later reader wonder what the design used to say. Omit the
subsection when the change was purely additive.}

**Lands in idea.md as:** {which section or Decision row now carries the result}

---
