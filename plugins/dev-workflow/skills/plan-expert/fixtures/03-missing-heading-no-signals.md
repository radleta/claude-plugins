## Step: Fix indentation inconsistency in plan-expert SKILL.md

Update the indentation of the bullet list under `### 8. Match the Artifact` in `plan-expert/SKILL.md`. Some items use 2-space indentation and others use 4-space. Standardize all nested bullets to 2-space indentation to match the rest of the document.

No logic change — purely formatting and whitespace. The rendered markdown output is identical; this only affects raw file consistency.
